# Baked to /root/.my.cnf in the dev tier so the mysql client connects to the
# compose 'mysql' service with no per-repo config. Shared by every devcontainer;
# kept out of the production image (which has no mysql client). Override per
# invocation with -h/--ssl-* flags if needed.
[client]
host=mysql
ssl-verify-server-cert=off
