# This build context should only ever ship the Dockerfile and start-dockerd.
# Ignore everything, then re-include the one file COPY'd into the image, so a
# stray secret/.env dropped into dev/src can never leak into the dev tier even
# if a future COPY widens its scope.
*
!start-dockerd
